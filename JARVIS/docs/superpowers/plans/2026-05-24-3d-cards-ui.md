# 3D Aesthetic Subject Cards Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the Attendance Tracker subject cards from a transparent "backlogger" style to a solid, modern, 3D aesthetic box that fits the Scholar Atlas theme.

**Architecture:** Solid background components with multi-layered CSS shadows and vertical translation transforms for depth simulation.

**Tech Stack:** React, Tailwind CSS, Framer Motion (for transitions).

---

### Task 1: Refactor SubjectCard Styling

**Files:**
- Modify: `My Websites/scholar-atlas/src/components/attendance/SubjectCard.tsx:120-130`

- [ ] **Step 1: Apply 3D Box Styling**
Replace the transparent glassmorphism classes with solid backgrounds and 3D shadows.

```tsx
// Find the main container div in SubjectCard.tsx
// Old:
// <div className={`group relative bg-bg-surface/70 backdrop-blur-xl rounded-[32px] border-y border-r border-border-strong border-l-4 ${healthColors[stats.healthStatus]} shadow-[0_10px_30px_rgba(0,0,0,0.02)] overflow-hidden transition-all hover:shadow-[0_20px_50px_rgba(0,0,0,0.06)] h-fit dark:bg-bg-elevated/70`}>

// New (Modern 3D Aesthetic):
<div className={`
  group relative 
  bg-white dark:bg-zinc-900 
  rounded-[24px] 
  border-2 border-zinc-200 dark:border-zinc-800 
  border-l-[6px] ${healthColors[stats.healthStatus]}
  shadow-[0_8px_0_0_rgba(0,0,0,0.05)] dark:shadow-[0_8px_0_0_rgba(0,0,0,0.2)]
  hover:-translate-y-1 hover:shadow-[0_12px_0_0_rgba(0,0,0,0.08)] dark:hover:shadow-[0_12px_0_0_rgba(0,0,0,0.3)]
  transition-all duration-300 ease-out
  overflow-hidden h-fit
`}>
```

- [ ] **Step 2: Adjust Health Border Colors**
Ensure the left border accent remains vibrant against the solid background. (Verify `healthColors` in the same file).

- [ ] **Step 3: Commit UI Changes**
```bash
git add "My Websites/scholar-atlas/src/components/attendance/SubjectCard.tsx"
git commit -m "feat: implement 3D aesthetic subject cards for attendance tracker"
```

---

### Task 2: Global Push to Git

**Files:**
- [x] All previous rebranding changes
- [ ] New 3D UI changes

- [ ] **Step 1: Final verification of all changes**
Run a quick grep to ensure no "backlogger-buddy" strings remain.

- [ ] **Step 2: Push to remote repository**
```bash
git push
```
